@model ManageSellerProductsViewModel
@{
    ViewData["Title"] = "Manage Products";
}
<h1>Manage Products</h1>
<div class="container">
    <div class="row">
        @foreach (var p in Model.Products)
        {
            <div class="col-6 col-md-3">
                <partial name="_ProductEditCard" model="p" />
            </div>
        }
    </div>

</div>
